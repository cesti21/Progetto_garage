package controller;

public class Thread {
	/* Utilizzare i semafori per i thread
	 * Mettere il garage in mutua esclusione 
	 * Fare una 'foto' al garage, farne una copia e salvarla in un'altra classe(facciamo questo per evitare di 
	 * 																			mettere in pausa l'esecuzione del programma,sempre per 
	 * 																			questo motivo utilizziamo i semafori) 
	 * Ricavare da questa classe i dati che ci interessano(scelti da noi)
	 * Infine stamperemo questi dati quando richiesto
	 */
}
